{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && echo '=== Conteúdo da pasta dist ===' && ls -la dist/ && echo '=== Fim do conteúdo ===' && pwd"
  },
  "deploy": {
    "startCommand": "ls -la && cd /app && ls -la dist/ && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}